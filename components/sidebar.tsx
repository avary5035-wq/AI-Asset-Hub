"use client";
import { useState } from "react";
import { Archive, Boxes, Building2, ChevronLeft, Clapperboard, FolderKanban, Heart, Home, ImageIcon, Settings, Sparkles, Users, X } from "lucide-react";
type Page = "dashboard" | "library" | "projects" | "team" | "settings";
const nav = [{ label: "Dashboard", icon: Home, page: "dashboard" }, { label: "Asset Library", icon: ImageIcon, page: "library" }];
export function Sidebar({ page, setPage }: { page: Page; setPage: (p: Page) => void }) { const [open, setOpen] = useState(false); const [collapsed, setCollapsed] = useState(false); const content = <aside className={`flex h-full flex-col border-r border-line bg-white transition-all ${collapsed ? "w-20" : "w-64"}`}><div className="flex h-[72px] items-center justify-between px-5"><div className="flex items-center gap-2 overflow-hidden"><span className="grid h-8 w-8 shrink-0 place-items-center rounded-lg bg-blue-600 text-white"><Sparkles size={17}/></span>{!collapsed && <span className="whitespace-nowrap font-semibold tracking-tight">AI Asset Hub</span>}</div><button className="hidden text-slate-400 lg:block" onClick={() => setCollapsed(!collapsed)}><ChevronLeft size={17} className={collapsed ? "rotate-180" : ""}/></button><button className="lg:hidden" onClick={() => setOpen(false)}><X size={18}/></button></div><nav className="space-y-1 px-3">{nav.map(({label, icon: Icon, page: p}) => <button key={label} onClick={() => setPage(p)} className={`nav-item w-full ${page === p ? "nav-active" : ""}`}><Icon size={18}/>{!collapsed && label}</button>)}<p className={`px-3 pt-6 text-[11px] font-semibold uppercase tracking-wider text-slate-400 ${collapsed ? "hidden" : ""}`}>Asset types</p>{[["Characters",Users],["Scenes",Clapperboard],["Props",Boxes],["Wardrobe",Archive],["Creatures",Sparkles],["Vehicles",Building2]].map(([label, Icon]) => <button key={String(label)} onClick={() => setPage("library")} className="nav-item w-full">{typeof Icon !== "string" && <Icon size={18}/>} {!collapsed && label}</button>)}<p className={`px-3 pt-6 text-[11px] font-semibold uppercase tracking-wider text-slate-400 ${collapsed ? "hidden" : ""}`}>Workspace</p>{[["My Projects",FolderKanban,"projects"],["Favorites",Heart,"library"],["Team",Users,"team"],["Settings",Settings,"settings"]].map(([label, Icon, p]) => <button key={String(label)} onClick={() => setPage(p as Page)} className={`nav-item w-full ${page === p ? "nav-active" : ""}`}>{typeof Icon !== "string" && <Icon size={18}/>} {!collapsed && label}</button>)}</nav><div className="mt-auto border-t border-line p-3"><div className="flex items-center gap-2 rounded-lg p-2"><div className="grid h-8 w-8 place-items-center rounded-full bg-slate-900 text-xs font-bold text-white">DC</div>{!collapsed && <div className="min-w-0"><p className="truncate text-sm font-medium">David Chen</p><p className="truncate text-xs text-slate-500">NOVA Studio</p></div>}</div></div></aside>; return <><button onClick={() => setOpen(true)} className="fixed left-4 top-4 z-30 grid h-10 w-10 place-items-center rounded-lg border bg-white lg:hidden"><Sparkles size={17}/></button><div className="hidden h-screen lg:block">{content}</div>{open && <div className="fixed inset-0 z-40 bg-slate-950/30 lg:hidden"><div className="h-full w-72">{content}</div></div>}</> }
