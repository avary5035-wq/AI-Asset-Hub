"use client";
import { Heart, MoreHorizontal } from "lucide-react";
import type { Asset } from "@/lib/data";
export function Status({ status }: { status: Asset["status"] }) { const c = { Ready: "bg-emerald-500", Review: "bg-amber-500", Draft: "bg-orange-500", Archived: "bg-slate-400" }[status]; return <span className="pill border-0 bg-slate-50"><i className={`h-1.5 w-1.5 rounded-full ${c}`}/>{status}</span>; }
export function AssetCard({ asset, onOpen }: { asset: Asset; onOpen: (a: Asset) => void }) { return <article onClick={() => onOpen(asset)} className="group cursor-pointer overflow-hidden rounded-xl border border-line bg-white transition hover:-translate-y-0.5 hover:shadow-soft"><div className="relative aspect-[4/3] overflow-hidden bg-slate-100"><img src={asset.image} alt={asset.name} className="h-full w-full object-cover transition duration-500 group-hover:scale-105"/><div className="absolute inset-x-0 bottom-0 flex translate-y-full justify-between bg-gradient-to-t from-slate-950/70 px-3 pb-3 pt-8 transition group-hover:translate-y-0"><button className="rounded-md bg-white/95 px-2.5 py-1.5 text-xs font-semibold text-slate-800">Preview</button><span className="flex gap-1"><button className="grid h-7 w-7 place-items-center rounded-md bg-white/95 text-slate-700"><Heart size={14}/></button><button className="grid h-7 w-7 place-items-center rounded-md bg-white/95 text-slate-700"><MoreHorizontal size={14}/></button></span></div></div><div className="p-3"><div className="mb-2 flex items-start justify-between gap-2"><div><h3 className="truncate text-sm font-semibold">{asset.name}</h3><p className="mt-0.5 text-xs text-slate-500">{asset.type}</p></div><Status status={asset.status}/></div><div className="flex flex-wrap gap-1">{asset.tags.slice(0,3).map(tag => <span className="rounded bg-slate-100 px-1.5 py-0.5 text-[11px] text-slate-600" key={tag}>{tag}</span>)}</div></div></article> }
