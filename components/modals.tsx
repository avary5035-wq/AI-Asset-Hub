"use client";
import { useState } from "react";
import { Check, CloudUpload, LoaderCircle, Plus, Trash2, X } from "lucide-react";
import type { Asset } from "@/lib/data";
import { Status } from "./asset-card";
export function UploadModal({ close, complete }: { close:()=>void; complete:()=>void }) { const [uploaded,setUploaded]=useState(false); return <div className="fixed inset-0 z-50 grid place-items-center bg-slate-950/40 p-4"><div className="w-full max-w-2xl rounded-2xl bg-white p-6 shadow-2xl"><div className="flex justify-between"><div><h2 className="text-xl font-semibold">Upload assets</h2><p className="mt-1 text-sm text-slate-500">AI will analyze and tag your files automatically.</p></div><button onClick={close}><X/></button></div><button onClick={()=>setUploaded(true)} className="mt-6 grid min-h-56 w-full place-items-center rounded-xl border-2 border-dashed border-blue-200 bg-blue-50/40 transition hover:bg-blue-50"><div><CloudUpload className="mx-auto text-blue-600" size={34}/><p className="mt-3 font-semibold">Drop images or videos here</p><p className="mt-1 text-sm text-slate-500">or click to browse · JPG, PNG, WEBP, MP4, MOV</p></div></button>{uploaded && <div className="mt-4 rounded-lg border p-3"><div className="flex items-center gap-3"><div className="h-10 w-10 rounded bg-slate-100"/><div className="flex-1"><p className="text-sm font-medium">black_axe_concept.png</p><div className="mt-1 h-1.5 overflow-hidden rounded bg-slate-100"><div className="h-full w-full bg-blue-600"/></div></div><span className="text-xs text-emerald-600">Ready for AI analysis</span></div></div>}<div className="mt-6 flex justify-end gap-2"><button className="icon-button w-auto px-4" onClick={close}>Cancel</button><button disabled={!uploaded} onClick={complete} className="button">Analyze assets</button></div></div></div> }
export function DetailModal({ asset, close }: { asset:Asset; close:()=>void }) { const [tags,setTags]=useState(asset.tags); const [input,setInput]=useState(""); const add=()=>{if(input.trim()){setTags([...tags,input.trim()]);setInput("")}}; return <div className="fixed inset-0 z-50 overflow-y-auto bg-slate-950/40 p-4"><div className="mx-auto my-4 grid max-w-6xl overflow-hidden rounded-2xl bg-white shadow-2xl lg:grid-cols-[1.7fr_1fr]"><div className="relative min-h-[350px] bg-slate-950"><img src={asset.image} alt={asset.name} className="h-full min-h-[350px] w-full object-cover"/><button onClick={close} className="absolute left-4 top-4 grid h-9 w-9 place-items-center rounded-full bg-white/90"><X size={17}/></button></div><div className="p-6"><p className="text-sm text-blue-600">{asset.type}</p><h2 className="mt-1 text-2xl font-semibold">{asset.name}</h2><div className="mt-3"><Status status={asset.status}/></div><dl className="mt-7 space-y-4 text-sm"><div className="flex justify-between border-b pb-3"><dt className="text-slate-500">Project</dt><dd className="font-medium">{asset.project}</dd></div><div className="flex justify-between border-b pb-3"><dt className="text-slate-500">Uploaded by</dt><dd className="font-medium">{asset.uploadedBy}</dd></div><div className="flex justify-between border-b pb-3"><dt className="text-slate-500">Usage count</dt><dd className="font-medium">{asset.used} times</dd></div></dl><h3 className="mt-7 font-semibold">Primary tags</h3><div className="mt-3 flex flex-wrap gap-2">{tags.map((t,i)=><span className="pill" key={t}>{t}<button onClick={()=>setTags(tags.filter((_,n)=>n!==i))}><Trash2 size={12}/></button></span>)}</div><div className="mt-3 flex gap-2"><input value={input} onChange={e=>setInput(e.target.value)} onKeyDown={e=>e.key==="Enter"&&add()} placeholder="Add a tag" className="min-w-0 flex-1 rounded-lg border px-3 text-sm outline-blue-600"/><button onClick={add} className="icon-button"><Plus size={16}/></button></div><button onClick={close} className="button mt-7 w-full"><Check size={16}/>Save changes</button></div></div></div> }
export function Analysis({ close }: { close:()=>void }) { const [step,setStep]=useState(0); const steps=["Image Recognition","Asset Type Detection","Object Detection","Style Analysis","Tag Generation","Duplicate Detection"]; if(step<6) setTimeout(()=>setStep(step+1),450); return <div className="fixed inset-0 z-50 grid place-items-center bg-slate-950/40 p-4"><div className="w-full max-w-lg rounded-2xl bg-white p-6 shadow-2xl"><h2 className="text-xl font-semibold">AI Analysis</h2><p className="mt-1 text-sm text-slate-500">We&apos;re preparing your asset for the library.</p><div className="mt-6 space-y-3">{steps.map((s,i)=><div className="flex items-center gap-3 rounded-lg border p-3" key={s}>{i<step?<Check className="text-emerald-600" size={18}/>:i===step?<LoaderCircle className="animate-spin text-blue-600" size={18}/>:<span className="h-[18px] w-[18px] rounded-full border"/>}<span className="flex-1 text-sm font-medium">{s}</span><span className="text-xs text-slate-400">{i<step?"Completed":i===step?"Processing":"Pending"}</span></div>)}</div>{step>=6&&<div className="mt-6 rounded-lg bg-blue-50 p-4"><p className="font-semibold text-blue-900">AI result · Props · 99% confidence</p><p className="mt-2 text-sm text-blue-700">Weapons · Axe · Metal · Wood · Black · Vintage · Farming · Mafia</p><button onClick={close} className="button mt-4">Review asset</button></div>}</div></div> }
